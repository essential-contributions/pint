storage {
    counter: int,
}

intent Init {
    var value: int;
    state counter: int = storage::counter;
    constraint counter' == value;
}

intent Increment {
    state counter: int = storage::counter;
    constraint counter' == counter + 1;
}
