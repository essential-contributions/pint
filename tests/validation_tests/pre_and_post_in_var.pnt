// db <<<
// 1, 5
// >>>
storage {
    x: int,
    y: int
}

predicate foo() {
    constraint storage::x := 3;
    constraint storage::y := storage::y! + 1;

    constraint storage::x == nil;

    let x = storage::x'! + storage::y!;
    constraint x == 3 + 5;
    constraint x' == 3 + 6;
}
