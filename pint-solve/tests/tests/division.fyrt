var ::x: int;
var ::y: int;
var ::z: int;

constraint (::x >= 3);
constraint (::x <= 10);
constraint (::y >= 3);
constraint (::y <= 10);
constraint (::z >= 3);
constraint (::z <= 10);
constraint ((::x / ::y) > (::y / ::z));

solve satisfy;
