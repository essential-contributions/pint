interface Foo {
    intent A {
        pub var x: int;
        pub var x: int;
        pub var x: int;
        pub var y: int;
    }

    intent B {
        pub var z: int;
        pub var z: int;
        pub var z: int;
        pub var y: int;
    }
}

intent Simple {}

// parse_failure <<<
// symbol `x` has already been declared
// @47..48: previous declaration of the symbol `x` here
// @71..72: `x` redeclared here
// `x` must be declared or imported only once in this scope
// symbol `x` has already been declared
// @47..48: previous declaration of the symbol `x` here
// @95..96: `x` redeclared here
// `x` must be declared or imported only once in this scope
// symbol `z` has already been declared
// @165..166: previous declaration of the symbol `z` here
// @189..190: `z` redeclared here
// `z` must be declared or imported only once in this scope
// symbol `z` has already been declared
// @165..166: previous declaration of the symbol `z` here
// @213..214: `z` redeclared here
// `z` must be declared or imported only once in this scope
// >>>
