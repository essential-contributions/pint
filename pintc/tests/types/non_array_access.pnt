let a: real;

constraint a[0] == a[1];

solve satisfy;

// intermediate <<<
// var ::a: real;
// constraint (::a[0] == ::a[1]);
// solve satisfy;
// >>>

// typecheck_failure <<<
// expression for constraint must evaluate to a boolean
// @25..29: value must be an array or a storage map; found `real`
// attempt to index into a non-indexable value
// @14..37: expression for constraint must evaluate to a boolean
// >>>
