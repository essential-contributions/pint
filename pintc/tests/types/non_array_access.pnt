let a: real;

constraint a[0] == a[1];

solve satisfy;

// intermediate <<<
// var ::a: real;
// constraint (::a[0] == ::a[1]);
// solve satisfy;
// >>>

// typecheck_failure <<<
// attempt to index into a non-indexable value
// @25..29: value must be an array or a storage map; found `real`
// >>>
