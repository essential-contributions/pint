var a = 1..3;
var b:int = 3..5;

constraint a == b;

solve satisfy;

// parsed <<<
// var ::a;
// var ::b: int;
// constraint (::a >= 1);
// constraint (::a <= 3);
// constraint (::b >= 3);
// constraint (::b <= 5);
// constraint (::a == ::b);
// solve satisfy;
// >>>

// flattened <<<
// var ::a: int;
// var ::b: int;
// constraint (::a >= 1);
// constraint (::a <= 3);
// constraint (::b >= 3);
// constraint (::b <= 5);
// constraint (::a == ::b);
// solve satisfy;
// >>>
