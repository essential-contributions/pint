union Experience = Real | Unreal;

type Experience = bool;

// parse_failure <<<
// symbol `Experience` has already been declared
// @6..16: previous declaration of the symbol `Experience` here
// @40..50: `Experience` redeclared here
// `Experience` must be declared or imported only once in this scope
// >>>
