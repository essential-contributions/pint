let a = 11;
let b: int;
let c = false;

constraint b < (c ? 22 : 33);
constraint b > (a ? 44 : 55);

solve satisfy;

// intermediate <<<
// var ::a;
// var ::b: int;
// var ::c;
// constraint (::a == 11);
// constraint (::c == false);
// constraint (::b < (::c ? 22 : 33));
// constraint (::b > (::a ? 44 : 55));
// solve satisfy;
// >>>

// typecheck_failure <<<
// condition for select expression must be a `bool`
// @86..87: invalid type `int`, expecting `bool`
// constraint expression type error
// @70..98: constraint expression has unexpected type `Unknown`
// @70..98: expecting type `bool`
// >>>
