var x: int;

constraint x >= -10;
var obj: int = x*x + 2*x - 8;

solve minimize obj;

// parsed <<<<
// var ::x: int;
// var ::obj: int;
// constraint (::x >= -10);
// constraint (::obj == (((::x * ::x) + (2 * ::x)) - 8));
// solve minimize ::obj;
// >>>

// flattened <<<
// var ::x: int;
// var ::obj: int;
// constraint (::x >= -10);
// constraint (::obj == (((::x * ::x) + (2 * ::x)) - 8));
// solve minimize ::obj;
// >>>
