let x: real = 1.0..4.0;
let y: real = 1.0..4.0;

constraint x + y == 3.3;

solve satisfy;

// intermediate <<<
// var ::x: real;
// var ::y: real;
// constraint (::x >= 1e0);
// constraint (::x <= 4e0);
// constraint (::y >= 1e0);
// constraint (::y <= 4e0);
// constraint ((::x + ::y) == 3.3e0);
// solve satisfy;
// >>>

// flattened <<<
// var ::x: real;
// var ::y: real;
// constraint (::x >= 1e0);
// constraint (::x <= 4e0);
// constraint (::y >= 1e0);
// constraint (::y <= 4e0);
// constraint ((::x + ::y) == 3.3e0);
// solve satisfy;
// >>>
