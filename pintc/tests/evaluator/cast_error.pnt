const a = {11} as int;
const b = [22, 33] as int;
const c = 44 as bool;

solve satisfy;

// parsed <<<
// const ::b = [22, 33] as int;
// const ::a = {11} as int;
// const ::c = 44 as bool;
// solve satisfy;
// >>>

// typecheck_failure <<<
// invalid cast
// @33..48: illegal cast from a `int[_]`
// casts may only be made from an int to a real, from a bool to an int or from an enum to an int
// invalid cast
// @10..21: illegal cast from a `{int}`
// casts may only be made from an int to a real, from a bool to an int or from an enum to an int
// invalid cast
// @60..70: illegal cast from a `int`
// casts may only be made from an int to a real, from a bool to an int or from an enum to an int
// >>>
