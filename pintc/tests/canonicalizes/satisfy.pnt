let x = 1..10;
let z = 1..10;
constraint z * x == 10;
solve satisfy;

// intermediate <<<
// var ::x;
// var ::z;
// constraint (::x >= 1);
// constraint (::x <= 10);
// constraint (::z >= 1);
// constraint (::z <= 10);
// constraint ((::z * ::x) == 10);
// solve satisfy;
// >>>

// flattened <<<
// var ::x: int;
// var ::z: int;
// constraint (::x >= 1);
// constraint (::x <= 10);
// constraint (::z >= 1);
// constraint (::z <= 10);
// constraint ((::z * ::x) == 10);
// solve satisfy;
// >>>