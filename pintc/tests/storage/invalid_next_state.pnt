let y: int;
constraint y' == 5;
solve satisfy;


// intermediate <<<
// var ::y: int;
// constraint (::y' == 5);
// solve satisfy;
// >>>

// typecheck_failure <<<
// `next state` access must be bound to a state variable
// @23..24: `next state` access must be bound to a state variable
// attempt to index into a non-indexable value
// @12..30: expression for constraint must evaluate to a boolean
// >>>
