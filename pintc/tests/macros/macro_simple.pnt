macro @a_macro($a, $0, $type) {
    var $a: $type = $0;
    var b: $type;
    constraint b > $a;
    b
}

constraint @a_macro(id; 1234; int) > 1;

solve satisfy;

// intermediate <<<
// var ::id: int;
// var ::anon@0::b: int;
// constraint (::anon@0::b > 1);
// constraint (::id == 1234);
// constraint (::anon@0::b > ::id);
// solve satisfy;
// >>>

// flattened <<<
// var ::id: int;
// var ::anon@0::b: int;
// constraint (::anon@0::b > 1);
// constraint (::id == 1234);
// constraint (::anon@0::b > ::id);
// solve satisfy;
// >>>
