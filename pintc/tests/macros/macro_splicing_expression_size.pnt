macro @sum($x, &rest) {
    @sum($x + &rest)
}

macro @sum($x) {
    $x
}

var ary: int[10 + 1];

var a = @sum(~ary);

solve satisfy;

// parse_failure <<<
// unable to determine spliced array size
// @111..115: unable to determine spliced array size for `::ary` while parsing
// macro array splicing is currently limited to immediate integer sizes or enums
// expected `!`, `(`, `+`, `-`, `::`, `[`, `a literal`, `cond`, `constraint`, `enum`, `exists`, `false`, `forall`, `ident`, `if`, `interface`, `macro_name`, `solve`, `state`, `true`, `type`, `use`, `var`, `{`, or `}`, found `~ary`
// @111..115: expected `!`, `(`, `+`, `-`, `::`, `[`, `a literal`, `cond`, `constraint`, `enum`, `exists`, `false`, `forall`, `ident`, `if`, `interface`, `macro_name`, `solve`, `state`, `true`, `type`, `use`, `var`, `{`, or `}`
// @106..116: when making macro call to '::@sum'
// >>>
