var b = 3;
var c = 2;
var b1: int;
constraint b1 == b + c;
solve minimize b + c;
solve satisfy;
solve maximize b - c;

// parse_failure <<<
// `solve` directive must only appear once
// @59..79: previous declaration of the `solve` directive here
// @81..94: `solve` directive must only appear once
// `solve` directive must only appear once
// @59..79: previous declaration of the `solve` directive here
// @96..116: `solve` directive must only appear once
// >>>