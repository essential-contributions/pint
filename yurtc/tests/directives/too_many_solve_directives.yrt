let b = 3;
let c = 2;
let b1: int;
constraint b1 == b + c;
solve minimize b + c;
solve satisfy;
solve maximize b - c;

// parse_failure <<<
// `solve` directive must only appear once
// @81..94: `solve` directive must only appear once
// `solve` directive must only appear once
// @96..116: `solve` directive must only appear once
// >>>