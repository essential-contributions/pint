let a = 123;
let b = 123.456;
let c: real;

fn d(e: real) -> bool {
    e > 0.0
}

constraint d(c);
constraint c > a;
constraint c < b;

solve minimize c;

// ast <<<<
// let ::a = 123;
// let ::b = 1.23456e2;
// let ::c: real;
// fn ::d(e: real) -> bool { (::e > 0e0) };
// constraint ::d(::c);
// constraint (::c > ::a);
// constraint (::c < ::b);
// solve minimize ::c;
// >>>

// We'll get this error until we implement type inference.
// compile_failure <<<
// internal error: Found untyped variable.
// >>>
