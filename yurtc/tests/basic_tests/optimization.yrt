let x: int = 1..3;
let y: int = 1..3;

solve maximize x + y;

// intermediate <<<<
// var ::x: int;
// var ::y: int;
// constraint (var ::x: int >= 1);
// constraint (var ::x: int <= 3);
// constraint (var ::y: int >= 1);
// constraint (var ::y: int <= 3);
// solve maximize (::x + ::y);
// >>>

// solve_failure <<<
// internal error: pcp solver: only constraint satisfaction problems are currently supported 
// >>>
