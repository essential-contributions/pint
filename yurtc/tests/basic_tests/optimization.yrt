let x: int = 1..3;
let y: int = 1..3;

solve maximize x + y;

// intermediate <<<<
// var ::x: int;
// var ::y: int;
// constraint (::x >= 1);
// constraint (::x <= 3);
// constraint (::y >= 1);
// constraint (::y <= 3);
// solve maximize (::x + ::y);
// >>>

// solve_failure <<<
// solver internal error: (pcp) only constraint satisfaction problems are currently supported
// >>>
