let x: real = 1..4;
let y: real = 1..4;

constraint x + y == 3.3;

solve satisfy;

// intermediate <<<
// var ::x: real;
// var ::y: real;
// constraint (var ::x: real >= 1);
// constraint (var ::x: real <= 4);
// constraint (var ::y: real >= 1);
// constraint (var ::y: real <= 4);
// constraint ((::x + ::y) == 3.3e0);
// solve satisfy;
// >>>

// solve_failure <<<
// solver internal error: (pcp) only integer variables are currently supported
// >>>
