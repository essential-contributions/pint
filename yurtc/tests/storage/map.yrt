storage {
    supply: int,
    balances: (int => int),
}

intent Simple {
    state supply = storage::supply;
    state bal_0 = storage::balances[0];
    state bal_69 = storage::balances[69];

    constraint bal_0 > 0;
    constraint bal_69 > 0;
}
