// <disabled>
macro @m($a, $b, &rest) {
    let a = $a + $b;
    @m(&rest);
}

macro @m($a) {
    $a
}

// @m() has no signature with only 2 params.  The param pack can never be empty.
@m(1; 2; 3; 4);

solve satisfy;

// parse_failure <<<
// pattern failure
// >>>
