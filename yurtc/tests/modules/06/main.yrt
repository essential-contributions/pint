use b_mod::{s0, s1};
use ::b_mod::{s2, s3};
use b_mod::s4;

use b_mod::c_mod::A::{C, D};
use ::b_mod::c_mod::{A, A::B};

constraint s0 == B;
constraint b_mod::s1 == C;
constraint s2 == A::B;
constraint ::b_mod::s3 == b_mod::c_mod::A::C;
constraint s4 == ::b_mod::c_mod::A::B;

enum U = V | W;

// intermediate <<<
// var ::b_mod::s0;
// var ::b_mod::s1;
// var ::b_mod::s2;
// var ::b_mod::s3;
// var ::b_mod::s4;
// enum ::U = V | W;
// enum ::b_mod::c_mod::A = B | C | D;
// enum ::b_mod::X = Y | Z;
// constraint (::b_mod::s0 == ::b_mod::c_mod::A::B);
// constraint (::b_mod::s1 == ::b_mod::c_mod::A::C);
// constraint (::b_mod::s2 == ::b_mod::c_mod::A::B);
// constraint (::b_mod::s3 == ::b_mod::c_mod::A::C);
// constraint (::b_mod::s4 == ::b_mod::c_mod::A::B);
// constraint (var ::b_mod::s0 == ::b_mod::c_mod::A::B);
// constraint (var ::b_mod::s1 == ::b_mod::c_mod::A::C);
// constraint (var ::b_mod::s2 == ::b_mod::c_mod::A::B);
// constraint (var ::b_mod::s3 == ::b_mod::c_mod::A::C);
// constraint (var ::b_mod::s4 == ::b_mod::c_mod::A::B);
// >>>

// compile_failure <<<
// compiler internal error: Found untyped variable.
// >>>
