let a: int;

constraint a == b::b;

solve satisfy;

// intermediate <<<
// var ::a: int;
// var ::b::b;
// constraint (::a == ::b::b);
// constraint (var ::b::b == 1);
// solve satisfy;
// >>>

// We'll get this error until we implement type inference.
// compile_failure <<<
// internal error: Found untyped variable.
// >>>
