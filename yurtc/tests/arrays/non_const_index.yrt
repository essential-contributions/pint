let n: int;
let a: int[10];

constraint a[n] == 3;

solve satisfy;

// flattening_failure <<<
// attempt to use a non-constant value as an array index
// @42..43: this would need to be a constant
// >>>
